version: '3.8'

services:
  app:
    build: .
    container_name: tutor-app
    restart: always
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://10.67.16.3:3306/springboot
      SPRING_DATASOURCE_USERNAME: springapp
      SPRING_DATASOURCE_PASSWORD: mathuserpassword
